<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Student">
    <insert id="sign" parameterType="student">
        insert into student_table(studentNum, studentName, korean, english, math)
        values(#{studentNum}, #{studentName}, #{korean}, #{english}, #{math})
    </insert>

    <select id="findAll" resultType="student">
        select studentNum, studentName
        from student_table
    </select>

    <select id="findByNum" parameterType="int" resultType="student">
        select studentNum, studentName, korean, english, math
        from student_table
        where studentNum = #{studentNum}
    </select>

    <update id="update" parameterType="student">
        update student_table
        set korean = #{korean}, english = #{english}, math = #{math}
        where studentNum = #{studentNum}
    </update>

    <update id="delete" parameterType="int">
        delete from student_table
        where studentNum = #{num}
    </update>
</mapper>